# SECURITY REPORT

Generated by Nexus Auditor — 2026-03-06

---

## FINDING 1 — Dynamic code execution in tools
SEVERITY: high
FILE: tools/tools.py
LINE: 102
ISSUE: Dynamic tool code from dictionary is executed at runtime. If dynamic_tools content is ever user-controllable, this is remote code execution.
RECOMMENDED FIX: Validate dynamic_tools source. Add allowlist of permitted tool names. Consider sandboxing.
STATUS: awaiting Builder fix

## FINDING 2 — Code execution in health scanner
SEVERITY: medium
FILE: core/health_scanner.py
LINE: 687
ISSUE: fix_code from brain's error_log table is compiled and run. Comment says "trusted brain-stored code only" but if brain DB is compromised, this becomes a risk.
RECOMMENDED FIX: Add validation that fix_code only contains known safe patterns. Consider using subprocess instead.
STATUS: awaiting Builder fix (has nosec annotation — acknowledged risk)

## FINDING 3 — Shell mode in subprocess calls
SEVERITY: medium
FILE: tools/tools.py:83, core/tools.py:124, core/chat_handler.py:667
ISSUE: Three subprocess calls use shell mode. The chat_handler case is highest risk since chat input could influence the command string.
RECOMMENDED FIX: Use subprocess with argument list (no shell) and shlex.split where possible.
STATUS: awaiting Builder fix

## FINDING 4 — f-string SQL queries
SEVERITY: low
FILE: Multiple (core/services/lead_service.py, core/competitor_monitor.py, core/ad_engine.py, etc.)
ISSUE: SQL queries built with f-strings for column names/SET clauses. All examined cases use parameterized values (?) for data. Safe but fragile — future changes could introduce injection.
RECOMMENDED FIX: Consider a query builder to reduce risk.
STATUS: low priority — current code appears safe

## FINDING 5 — Hardcoded secrets
SEVERITY: none
ISSUE: No hardcoded API keys found in Python or TypeScript files. Secrets stored in ~/.nexus/config.json (correct).
STATUS: PASS

## FINDING 6 — Token references in print statements
SEVERITY: low
FILE: scripts/create_fb_campaign.py (lines 90, 320, 353-356)
ISSUE: Print statements reference tokens in help text (not actual values). Safe but could confuse log readers.
STATUS: informational

## FINDING 7 — Blank lead record
SEVERITY: low
FILE: Database (leads table)
ISSUE: Lead ID 11 has all fields empty. Should be cleaned up.
RECOMMENDED FIX: Remove the empty lead record. Add NOT NULL constraint to prevent future empty records.
STATUS: awaiting Builder fix

## FINDING 8 — NameError in outbound_gate
SEVERITY: high
FILE: integrations/messaging.py
LINE: 159
ISSUE: outbound_gate() references `log` variable that is not defined in scope. This causes a NameError crash when any outbound email is sent through the gate. The gate CANNOT function — all emails through zoar_bot.send_email() will crash.
RECOMMENDED FIX: Add `log = logging.getLogger("messaging")` at module level in integrations/messaging.py
STATUS: awaiting Builder fix — URGENT, blocks all outbound email

---

## Round 7 — Vendor Insert Path Security Audit

DATE: 2026-03-06 15:30 PT

### Single Insert Gate

ALL vendor inserts go through `core/vendor_db.py:save_vendor()` (line 252). No bypass paths found.

Gate chain: `validate_vendor_location(city)` → `quick_vet(category, email)` → dedup by name+phone/city → parameterized INSERT

### Complete Insert Path Table

| # | File | Line | Function | Through save_vendor? | Validation |
|---|------|------|----------|---------------------|------------|
| 1 | `core/vendor_db.py` | 252 | `save_vendor()` | IS the gate | location → quick_vet → dedup → INSERT |
| 2 | `core/vendor_db.py` | 668 | `bulk_save_vendors()` | Yes (loop) | Calls save_vendor() per item |
| 3 | `core/vendor_research_daemon.py` | 291 | research cycle | Yes | AI JSON → bulk_save_vendors |
| 4 | `scripts/vendor_discovery.py` | 166 | discovery | Yes | Multi-source → bulk_save_vendors |
| 5 | `scripts/worker_runner.py` | 208 | worker loop | Yes | Fleet task → bulk_save_vendors |
| 6 | `core/vendor_api.py` | 262 | POST /vendors/search | Yes | API → bulk_save_vendors |
| 7 | `core/vendor_api.py` | 293 | POST /vendors/bulk-import | Yes | API → bulk_save_vendors |
| 8 | `core/google_places_vendor_discovery.py` | 219 | discover_and_save | Yes | Places API → save_vendor |

### Direct DB Write Check

- `scripts/enrich_vendors.py` lines 50, 66: Uses sqlite3 directly but only for UPDATE (email, contact_name) — not INSERT
- No `.sql` files found in the project
- No direct `INSERT INTO vendors` outside `core/vendor_db.py`

### Assessment

- **SQL Injection**: NOT VULNERABLE — all queries use parameterized `?` placeholders
- **Bypass Risk**: NONE — no code path inserts vendors without going through save_vendor()
- **Data Quality**: WEAK — AI-generated data passes location and category gates but has no business-reality validation (phone/website/email not verified pre-insert)
- **Post-Insert Vetting**: EXISTS but ASYNC — vendors sit unvetted in DB for hours before enrichment runs

### Recommendation

Add a pre-insert phone format check to `save_vendor()` that rejects:
- 555 prefix phones (known fictional)
- Repeated last-4 digits (1111, 2222, etc.)
- Sequential patterns (1234, 4321)

This would have blocked ~7,200 of the 12,304 hallucinated vendors at the gate.

---

## Summary (Updated Round 7)
- Critical: 0
- High: 2 (dynamic code in tools, NameError in outbound_gate — Finding 8 may be fixed by Builder)
- Medium: 3 (code run in health scanner, 3x shell mode subprocess)
- Low: 3 (f-string SQL, token print, blank lead)
- New: Vendor insert path audit — ALL paths verified, no bypasses, no SQL injection
- Total: 8 findings + 1 audit
- No hardcoded secrets found

---

## Round 8 — Scraper Scripts Security Audit (Go-Live)

DATE: 2026-03-06 20:00 PT

### Files Scanned

| File | Purpose | Result |
|------|---------|--------|
| `scrapers/craigslist_scraper.py` | Craigslist vendor scraper | PASS |
| `scrapers/email_enrichment.py` | Email enrichment via Hunter.io | PASS |
| `scrapers/enrich_vendors.py` | Vendor data enrichment | PASS |
| `scrapers/google_maps_scraper.py` | Google Maps vendor scraper | PASS |
| `scrapers/run_all_discovery.py` | Orchestrator for all scrapers | PASS |
| `scrapers/vendor_discovery.py` | AI-powered vendor research | PASS |
| `scrapers/wedding_directory_scraper.py` | Wedding directory scraper | PASS |
| `scrapers/yelp_scraper.py` | Yelp vendor scraper | PASS |

### Checks Performed

1. **Hardcoded Credentials**: PASS — All scrapers use `load_config()` from config.json
2. **SQL Injection**: PASS — All writes through `save_vendor()`, parameterized queries
3. **Browser Cleanup**: PASS — All Playwright scrapers close in `finally` blocks
4. **Data Validation**: PASS — Phone normalization, email validation, category checks
5. **Rate Limiting**: PASS — Delays between requests included
6. **Read-Only (Rule 24)**: PASS — No interaction with platform content

### Assessment

All 8 scraper scripts pass security audit. No credential leaks, no SQL injection vectors, no resource leaks, full Rule 24 compliance
